import SwiftUI
import Utility

public extension Color {
    // groundColor
    static let primaryLightGroundColor = hex(0xFDFDFD)
    static let accentLightGroundColor = hex(0xF6F7F8)
    static let primaryDarkGroundColor = hex(0x161F29)
    static let accentDarkGroundColor = hex(0x0F151D)
    static let searchLigthGroundColor = hex(0xE6EAED)
    static let searchDarkGroundColor = hex(0x10151B)

    // textColor
    static let primaryLightTextColor = hex(0x131518)
    static let accentLightTextColor = hex(0x5E6E80)
    static let primaryDarkTextColor = hex(0xFFFFFF)
    static let accentDarkTextColor = hex(0x8A97A3)

    // 共通カラー
    static let favoriteRed = hex(0xDB1E52)
}
